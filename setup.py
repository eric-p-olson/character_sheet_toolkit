from setuptools import setup, find_packages

setup(
    name='character_sheet_toolkit',
    version='0.1dev0',
    author='Eric Olson',
    author_email='olsonep@gmail.com',
    #packages=find_packages(exclude=["tests"]),
    install_requires=[

    ],
    long_description=open('README.md').read()
)